# 2026-08-28 本机会话清理记录

用户确认清理此前测试、演示产生的会话，今后正常创建的会话继续持久化。此次仅维护本机 `pf_nl2sql` 数据，不变更应用启动逻辑或数据库迁移。

## 范围与保护

- 以此前检查得到的166个旧会话为清理范围，创建时间不晚于 `2026-08-28 10:06:34.440`，最后更新时间早于 `2026-08-28 10:08:00`。时间条件只用于生成一次性清单，实际删除按固定会话编号执行。
- 先冻结会话编号、用户编号、版本、更新时间和关联任务清单；备份后，在事务内依次锁定会话和任务并重新比对。会话或任务有变化、任务仍在执行时跳过，不扩大范围。
- 清理期间用户新建的2个会话不在清单内，均予以保留；未停止正在使用的后端服务。
- 完整保留客户、交易、产品持有、营销数据、账号、业务术语、SQL核查日志及审计事件。

## 备份

本机备份位于 `tmp/conversation-cleanup-20260828/`，该目录已由仓库忽略规则排除，不提交到Git。

| 文件 | 用途 |
| --- | --- |
| backup.sql | 清理前六张相关表的数据备份，包含审计事件；无建表或删表语句 |
| backup.sha256 | 备份完整性校验；执行前已核对 |
| sessions.tsv、tasks.tsv | 固定的清理清单及版本快照 |
| protected-sessions.tsv | 准备清理时已发现的新用户会话清单 |
| committed-report.txt | 事务提交后的清理数量 |
| verification.tsv | 清理后的数据验证结果 |

备份共1,985,610字节，SHA-256为 `2e9b73b6ec351f81ff29bbfdbb2dba0887a600ae296e85582e45b43b0f062259`。包含模拟查询结果和审计信息，应按本地备份管理；需要恢复时应先导入隔离数据库，再按清单提取需要的记录，不要向正在使用的数据库直接整份导入。

## 执行结果

| 对象 | 清理数量 |
| --- | ---: |
| conversation_session | 166 |
| query_task | 190 |
| conversation_message | 321 |
| query_task_event | 756 |
| query_history | 181 |

版本或状态变化导致的跳过数为0。SQL日志和 `audit_event` 未执行删除。

## 验证

- 清单范围内旧会话剩余0条，未出现孤立消息、事件或查询历史。
- 通过正在运行的后端实际读取会话API：客户经理和团队负责人均返回空列表；机构负责人返回用户新创建的2个会话，消息接口分别可读取18条、4条消息（记录的是验证时刻，后续可继续增长）。
- 客户10,002条、交易200,000条、产品持有20,000条、营销关系30,000条、账号3条，清理前后数量一致。审计记录保留，用户继续操作时正常增加。
- 未调用真实模型、未创建新的验收会话、未重启用户正在运行的服务。前端已加载的旧列表可通过“刷新会话历史”或刷新页面更新。

之后的集成测试和HTTP验收应使用独立测试数据库及后端实例，避免再次向日常使用的会话历史写入验收记录。正常启动、重启或重新登录仍保留用户会话。
