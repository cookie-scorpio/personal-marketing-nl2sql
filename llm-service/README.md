# 大模型服务

本目录管理自然语言理解、查询要素抽取、完备性校验、主动澄清、NL2SQL、SQL 安全校验和效果评测。

- `app/`：可部署服务代码。
- `prompts/`：提示词及版本说明。
- `evaluation/`：脱敏标准问题集、预期结果和评测工具。
- `tests/`：单元、集成和安全测试。

模型输出不得直接获得生产数据库执行权限。只读限制、对象白名单、用户权限和查询成本控制必须在可信服务端再次校验。
