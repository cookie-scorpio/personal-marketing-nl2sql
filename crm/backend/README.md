# CRM 后端

Spring Boot 后端代码放置在本目录。建议按 `customer`、`conversation`、`marketing`、`knowledge`、`authorization` 等业务模块分包，每个模块内部包含 `api`、`application`、`domain` 和 `infrastructure`。

首次初始化时请使用组织批准的 JDK、Spring Boot 和依赖版本，并补充 Maven 构建、配置、启动、测试和数据库变更说明。
