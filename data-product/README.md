# 大数据产品

本目录管理个金营销数据的在线查询服务、元数据能力、离线及实时任务、SQL 和数据质量规则。

v1.5 使用 MySQL 8.4 虚构业务库，建表迁移和查询实现位于 `crm/backend`。当前查询统一使用 MySQL，不需要启动 Spark SQL、Hive 或独立数据服务。

所有任务必须说明数据来源、输出、调度周期、负责人、口径版本和回退方式。仓库内只允许脱敏测试数据，不得保存生产查询结果。
