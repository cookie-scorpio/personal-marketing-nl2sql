# 项目协作说明

## 文档

- README 使用自然、易懂的中文，先说明用途，只保留主要功能，并清楚写明安装、配置、运行、部署、注意事项和许可。
- 避免使用“故事圣经”“实验性适配器”“流水线”“演练”等机械化或过度 AI 化表达。

## PowerShell

- 复合 `foreach` 或 `ForEach-Object` 的输出需要继续进入管道时，先保存到变量，再对变量使用管道，避免空管道解析错误。
- 调用 `rg` 时用目录参数配合 `-g '*.ext'`，不要把 Windows 路径末尾的通配符直接作为文件参数。
- Maven 的逗号测试列表在 PowerShell 中作为一个加引号参数传入，例如 `'-Dtest=A,B'`。

## 数据库迁移

- 已发布的 Flyway 迁移文件不可修改，包括注释和空白；新增变化必须创建后续迁移，禁止通过 `repair` 掩盖代码库与已部署数据库的差异。
