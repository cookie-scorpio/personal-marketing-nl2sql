# 客户关系管理系统

本目录包含个金营销 NL2SQL 平台的用户界面和业务后端。

- `frontend/`：登录和工号注册、多身份切换、对话查询、按身份隔离的会话历史、质量审计入口、权限管理和结果展示页面。
- `backend/`：统一认证接入、角色授权、身份与数据范围校验、会话管理、业务处理和审计接口。

前后端通过 `contracts/openapi/` 中的接口定义协作，不直接复制请求和响应结构。
