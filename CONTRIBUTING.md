# 团队协作规范

## 分支

- `main`：始终保持可构建、可测试，设置为保护分支。
- `feat/<范围>-<简述>`：功能开发，例如 `feat/llm-completeness-check`。
- `fix/<范围>-<简述>`：缺陷修复。
- `release/<版本>`：发布稳定分支，例如 `release/1.2.0`。
- `hotfix/<简述>`：生产紧急修复。

原则上不设置长期 `develop` 分支。开发分支应尽量短，及时通过 Pull Request 合并，减少三个团队之间长期分叉。如果现有发布制度要求固定联调分支，可再增加受保护的 `develop`。

## 提交信息

格式：`类型(范围): 简要说明`

示例：

```text
feat(crm): 增加客户关系查询接口
fix(data): 修复统计日期边界
test(llm): 增加歧义术语评测用例
docs(contract): 更新查询接口字段说明
```

常用类型：`feat`、`fix`、`refactor`、`test`、`docs`、`build`、`chore`。

## Pull Request

1. 一个 PR 只解决一个主题，关联对应任务或缺陷。
2. 至少一名对应模块维护人评审；跨系统契约变更需上下游维护人共同评审。
3. 合并前必须通过构建、单元测试、静态检查和必要的安全检查。
4. 数据库脚本必须可追踪，说明执行顺序、影响范围和回退方式。
5. 禁止提交密钥、生产配置、真实客户数据、未脱敏日志和大型二进制文件。

## 目录归属

| 目录 | 建议责任团队 | 合并要求 |
|---|---|---|
| `crm/` | CRM 前后端团队 | CRM 模块维护人评审 |
| `data-product/` | 大数据团队 | 数据模块维护人评审 |
| `llm-service/` | 大模型团队 | 大模型模块维护人评审 |
| `contracts/` | TL 与相关上下游 | 至少两个受影响团队评审 |
| `deploy/` | TL/运维 | 应用维护人和运维评审 |

在团队 Gitee 用户名确定后，由仓库管理员在根目录增加 `CODEOWNERS`，将上述目录绑定到实际维护人。

## 版本

发布标签使用语义化版本，例如 `v1.2.0`。每次发布应记录功能、修复、配置变化、数据库变化和回退说明。
